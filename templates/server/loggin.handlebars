<!--<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link rel="stylesheet" href="styles.css">
  <link rel="stylesheet" href="/bootstrap.min.css">
</head>
<body>-->
<main>
    {{#if failed }}
    <h4 class="bg-danger text-white p-2 text-center">
        Autenticación fallida, revisa tu usuario o contraseña.
    </h4>
    {{/if}}
    <div class="container vh-100 d-flex justify-content-center align-items-center">
        <form method="post" class="w-50">
            <div class="mb-3">
                <input type="text" placeholder="Usuario" name="username" class="form-control" value="{{username}}" />
            </div>
            <div class="mb-3">
                <input type="password" placeholder="Contraseña" name="password" class="form-control"
                    value="{{password}}" />
            </div>
            <button type="submit" class="primary-btn">Iniciar sesión</button>
        </form>
        <form action="/saveUser">
            <button type="submit" class="primary-btn">Registrarse</button>
        </form>

    </div>
    </body>
</main>

<!--</html>-->